"""
문제: 주사위게임1
레벨: 0
링크: https://school.programmers.co.kr/learn/courses/30/lessons/181839

[문제 요약]
1부터 6까지 숫자가 적힌 주사위가 두 개 있습니다. 두 주사위를 굴렸을 때 나온 숫자를 각각 a, b라고 했을 때 얻는 점수는 다음과 같습니다.

a와 b가 모두 홀수라면 a2 + b2 점을 얻습니다.
a와 b 중 하나만 홀수라면 2 × (a + b) 점을 얻습니다.
a와 b 모두 홀수가 아니라면 |a - b| 점을 얻습니다.
두 정수 a와 b가 매개변수로 주어질 때, 얻는 점수를 return 하는 solution 함수를 작성해 주세요.

[나의 풀이]
- def solution(a, b):
    if a % 2 and b % 2 == 1:
        return a*a + b*b
    elif a % 2 or b % 2 == 1:
        return 2 * (a + b)
    else:
        return abs(a-b)
ChatGPT의 말:
지금 코드는 겉으로는 맞게 보이지만 실제로는 논리 오류가 있어.
이 문제는 조건문에서 가장 많이 나오는 실수 패턴을 정확히 잡아야 한다.
❌ 핵심 오류: a % 2 and b % 2 == 1 은 네가 생각한 의미가 아니다
너의 의도는 분명:
a도 홀수이고, b도 홀수일 때
이걸 표현하려 한 거지?
하지만 파이썬에서는:

a % 2 and b % 2 == 1
이건 다음처럼 해석된다:
(a % 2) 이 truthy인가? → (0이 아니므로 true)
그리고 (b % 2 == 1) ?
즉, 왼쪽 조건(a % 2)은 그냥 홀수면 True로만 작동하고, “== 1 비교”가 적용되지 않는다.
즉, 너의 조건은 이렇게 되어버림:
a	b	a%2	b%2	실제 평가
홀수	짝수	True	0	False
홀수	홀수	True	1	True
짝수	홀수	0	1	False
짝수	짝수	0	0	False
→ “a 홀수 AND b 홀수”는 맞지만
⚠️ 코드를 잘못 쓰면 다음 elif가 완전히 무너진다.
❌ 두 번째 조건도 잘못됨
elif a % 2 or b % 2 == 1:
의도는:
둘 중 하나라도 홀수이면
이지만 실제 해석:
(a % 2) 가 truthy인가?  (즉 a가 홀수인가?)
or
(b % 2 == 1)
즉:
a가 홀수면 무조건 True
b만 홀수여도 True
a, b 둘 다 짝수일 때만 False
결국 이 elif는 거의 무조건 True가 되어 버리고
마지막 else는 실행되지 않게 됨.
⭐ 올바른 조건문 작성법
각 변수에 명확히 == 1 비교를 붙여야 한다.
👉 정답 구조
def solution(a, b):
    if a % 2 == 1 and b % 2 == 1:
        return a*a + b*b
    elif a % 2 == 1 or b % 2 == 1:
        return 2 * (a + b)
    else:
        return abs(a - b)
🎯 패턴 정리 (이번 문제에서 반드시 가져가야 할 것)
🔹 1. 논리 연산자 and/or 사용할 때는 비교를 반드시 양쪽에 붙인다
❌ 잘못된 예
a % 2 and b % 2 == 1
a % 2 or b % 2 == 1
✔ 올바른 예
a % 2 == 1 and b % 2 == 1
a % 2 == 1 or b % 2 == 1
항상 비교는 변수마다 독립적으로 넣어라.
🔹 2. 조건 우선순위는 다음처럼 구분된다
% (나머지)
==
and, or
그래서 괄호 없이 쓰면 의도와 다르게 해석된다.
🔹 3. 조건 분기 설계법
의도:
둘 다 홀수
하나만 홀수
둘 다 짝수
이럴 때 패턴은 항상:
if (둘 다 홀수)
elif (하나만 홀수)
else (둘 다 짝수)
이 구조는 패턴으로 익혀두면 모든 논리 문제에 바로 적용된다.
👍 최종 정답
def solution(a, b):
    if a % 2 == 1 and b % 2 == 1:
        return a*a + b*b
    elif a % 2 == 1 or b % 2 == 1:
        return 2 * (a + b)
    else:
        return abs(a - b)
원하면 이 조건 처리 패턴(AND/OR, 비교 우선순위, 논리 오류 피하는 법) 관련해서
실수하기 쉬운 문제 3개 묶어서 패턴 학습용 세트도 만들어줄게!

[다른 풀이 / 대안 접근]
- 다른 풀이 아이디어 또는 더 효율적인 방식

[접근 방법(Detail)]
- 아이디어 상세 설명
- 시간복잡도: O(n)

"""

def solution(입력변수):
    pass


if __name__ == "__main__":
    # 예시 테스트
    print(solution(샘플입력))